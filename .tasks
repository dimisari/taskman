- Adding and deleting categories
- check all the split at index and add catching functions for when expected pattern does not match because if it breaks it deletes all the task
  - pasted something from the terminal after "New Description" and it broke
  - It included a newline (that's probably the reason)
  - Unable to recreate the issue
- Add the ability to move around with arrows in the description of a task
  - When editing give the old description
- When inspecting the task show the parent task after adding
  - In other words keep showing the parent task after doing actions during inspection
- Only show the first 5 tasks
  - Add a mechanism to scroll to bigger number tasks if needed
- In the original menu when you don't enter a valid choice the program ends. Make it so that in shows the menu again after the message
